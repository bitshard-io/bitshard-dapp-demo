# URL of the BitShard wallet app (which hosts the popup bridge page).
# For local development, run the main bitshard frontend on :3000 and set this
# to http://localhost:3000.
VITE_BITSHARD_APP_URL=http://localhost:3000
